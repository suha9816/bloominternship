import React from 'react'

export default function testreact() {
    const [state, setstate] = useState(initialState)
    useEffect(() => {
        effect
        return () => {
            cleanup
        }
}, [input])
    return (
        <div>
            <h1>suha</h1>
        </div>
    )
}
